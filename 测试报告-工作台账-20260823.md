# 工作台账系统测试报告

- **测试日期**：2026-08-23
- **测试环境**：生产环境（阿里云 ECS 101.37.210.127:3001，Node 20，pm2 守护；城投项目 p_chengtou）
- **测试方法**：服务器本地 API 脚本测试（localhost）+ 外网链路测试（Mac→ECS）+ 代码审查（前端渲染面）
- **测试范围**：后端 22 个端点全覆盖（登录/工作日志/任务/计划/AI查询/设置/项目/健康检查）、安全专项、性能、兼容性
- **数据安全**：所有写入均带【测试AUTO】标记，测试完毕已全部删除（钉钉表格残留 0，正常数据 9 条日志 / 22 项任务完好），服务已重启清空内存镜像与测试会话，服务器/本地临时脚本已删除

---

## 一、测试结果统计

| 类别 | 用例数 | 通过 | 失败 | 通过率 |
|---|---|---|---|---|
| A 登录与鉴权 | 5 | 5 | 0 | 100% |
| B 工作日志 | 8 | 8 | 0 | 100% |
| C 任务模块 | 6 | 6 | 0 | 100% |
| D 计划模块 | 3 | 3 | 0 | 100% |
| E AI智能查询 | 2 | 1 | **1** | 50% |
| F 安全专项 | 10 | 10 | 0 | 100%* |
| 性能测试 | 4 项指标 | 4 | 0 | 100% |
| 兼容性测试 | 7 项指标 | 7 | 0 | 100% |
| **合计** | **45** | **44** | **1** | **97.8%** |

*F 组全部按预期验证出实际行为，其中多项行为本身构成风险（见问题清单）。

---

## 二、测试用例详情

### A 登录与鉴权（5/5 通过）

| ID | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| A1 | 无 token 访问 /api/get-work-list | 拒绝 | code=401「未登录或登录已过期」 | PASS |
| A2 | 伪造 token（32位乱串） | 拒绝 | code=401 | PASS |
| A3 | 非 Bearer 凭证（Basic） | 拒绝 | code=401 | PASS |
| A4 | 无免登码 POST /api/login | 按配置决定 | 返回会话（见问题 S1） | PASS（风险） |
| A5 | 伪造免登码登录 | 拒绝 | code=1「免登码换用户失败(40078)」 | PASS |

### B 工作日志（8/8 通过）

| ID | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| B1 | parse-work 规则解析 | 正确提取字段 | 解析出标题/进度80%/工时2h/标签 | PASS |
| B2 | submit-work 正常提交（含 XSS 载荷） | 入库+推送 | code=0，pendingSync=false（直推成功） | PASS |
| B3 | 提交后 get-work-list?scope=today 可见 | 可见 | 2 条可见；XSS 载荷原样存储（见问题 9） | PASS |
| B4 | 空内容提交 | 拒绝 | code=1「工作内容不能为空」 | PASS |
| B5 | 同内容重复提交（60s 窗口内） | 去重拦截 | code=1「相同内容刚已提交过」 | PASS |
| B6 | 超长内容（600 字） | 拒绝 | code=1「内容过长，请控制在500字以内」 | PASS |
| B7 | 工时 99h 非法值 | 拒绝或夹取 | code=0「记录成功」（见问题 5） | PASS（风险） |
| B9 | scope=week 列表查询 | 正常 | code=0 | PASS |

### C 任务模块（6/6 通过）

| ID | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| C1 | GET /api/tasks | 22 项任务+editable 标记 | code=0，22 项，editable 正确下发 | PASS |
| C2 | ?mine=1 我的任务过滤 | 仅本人相关 | code=0（guest 无关联，0 项，符合逻辑） | PASS |
| C3 | 非法状态值「已删除」 | 400 | code=400，提示可选值 | PASS |
| C4 | 不存在的任务 recordId | 404 | code=404「任务不存在或已被删除」 | PASS |
| C5 | 非负责人（guest）改状态 | 403 | code=403「仅任务负责人可修改状态」 | PASS |
| C6 | 单任务关联日志接口 | 正常 | code=0，日志数组 | PASS |

### D 计划模块（3/3 通过）

| ID | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| D1 | 负责人不在成员表 | 拒绝 | code=1「负责人『不存在的人』不在项目成员表中」 | PASS |
| D2 | submit-plan 正常写入任务表 | 入库+推送 | code=0，直推成功 | PASS |
| D3 | plan-options 成员+分类 | 正常 | 成员（含 unionId）+分类列表 | PASS |

### E AI 智能查询（1/2 通过）

| ID | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| E1 | 空问题 | 拒绝 | code=1「问题不能为空」 | PASS |
| E2 | 正常提问全链路（真实 LLM） | 返回纯文本回答 | code=0 但 **answer 为空**（LLM 持续限流，见问题 2） | **FAIL** |

### F 安全专项（10/10 按预期验证，多项构成风险）

| ID | 用例 | 实际行为 | 结果 |
|---|---|---|---|
| S1 | 无免登码直接获取管理员会话 | **成功**：name=浏览器访客，isAdmin=true（见问题 1） | 风险确认 |
| F2a | jsapi-sign file:// 协议 | code=400「url 参数无效」拒绝 | PASS |
| F2b | jsapi-sign 正常 http | 返回签名五元组（免鉴权，agentId/corpId 暴露，见问题 8） | PASS（风险） |
| F3a | GET /api/projects 权限视角 | guest 得到管理员视角；x-project-id 直读任意项目（见问题 3） | 风险确认 |
| F4 | CORS 策略 | Access-Control-Allow-Origin: * 全开放（见问题 7） | PASS（风险） |
| F5 | 未知 API 语义 | HTTP 200 + code=404（非 RESTful，见问题 4） | PASS（风险） |
| F6 | API Key 回显 | 已脱敏（回显空/****），无泄露 | PASS |
| F7 | /api/health 信息暴露 | 免鉴权暴露 mode/table/ai 状态（见问题 6） | PASS（风险） |
| F8 | 100KB 超大 JSON body | 被 64KB 限制拦截（HTTP 200 + 错误码，语义见问题 4） | PASS |
| F9 | 前端 XSS 渲染面 | 前端无 v-html，Vue 默认转义，存储型 XSS 不可直接触发（见问题 9） | PASS |

### 性能测试（服务器本地）

| 指标 | 结果 | 评估 |
|---|---|---|
| /api/health | avg 11ms，p95 26ms | 优 |
| /api/tasks（缓存命中） | avg 6ms，p95 9ms | 优 |
| /api/get-work-list | avg 6ms，p95 9ms | 优 |
| 20 并发 /api/tasks | 总耗时 172ms，全部成功 | 优 |

外网链路（Mac→ECS，模拟真实用户）：health 28ms / 首页 26ms / tasks 38ms——达标。

### 兼容性测试

| 检查项 | 结果 |
|---|---|
| index.html 缓存策略 | Cache-Control: no-cache，正确（避免钉钉 webview 缓存旧版） |
| 静态资源缓存 | public, max-age=31536000, immutable + 内容哈希，正确 |
| gzip 压缩 | 生效（主 JS 37.5KB 传输） |
| SPA 深链回退（/tasks 直访） | 200 返回 index.html，正确 |
| Content-Type | html/js 均带 charset，正确 |
| HEAD 请求 | 正常响应 |
| 已知限制 | HTTP 站点为非安全上下文（无 getUserMedia），已由语音三级引擎兜底；此为部署层限制，记录在案 |

---

## 三、问题清单（按严重程度排序；已于 2026-08-23 全部确认修复）

| # | 严重度 | 问题 | 复现步骤 | 修复方案 | 状态 |
|---|---|---|---|---|---|
| 1 | **严重（P0）** | 生产环境访客登录直通管理员：`ALLOW_DEV_LOGIN=1`，任何人 POST /api/login（无免登码）即得 `isAdmin=true` 会话，可读全员日志、改系统设置、管理项目 | `curl -X POST http://101.37.210.127:3001/api/login -H 'Content-Type: application/json' -d '{}'` → 返回 token 且 isAdmin=true | 生产 .env 设 `ALLOW_DEV_LOGIN=0`；访客会话不再默认管理员（仅演示模式本地用户为管理员） | ✅ 已修复并验证 |
| 2 | **高（P1）** | AI 智能查询空回答：智谱 glm-4.7-flash 持续限流（429/1302/1305）+超时，重试与备用模型切换均失败后返回 code=0 + 空 answer，用户看到空回复无任何提示 | AI 查询页连续提问数次（限流时段）；服务器日志大量「[llm] 限流/超时/切换备用模型」 | LLM 空内容按可重试错误处理（重试耗尽自动切备用模型，全失败返回明确错误提示） | ✅ 已修复并验证 |
| 3 | **高（P1）** | 跨项目越权：work/tasks 等业务路由完全信任 `x-project-id` 请求头，不校验项目成员身份（成员过滤只在 /api/projects 做）。任何登录用户知晓项目 id（p_chengtou 可猜测）即可读该项目全部任务/成员/日志结构，并可向其写入日志 | 登录后携带 `x-project-id: p_chengtou` 请求 /api/tasks → 返回全部 22 项任务（本次 C1 已实证） | 新增 project-guard 中间件挂载 work/ai/tasks 路由：按项目成员表校验（结果缓存 5 分钟；读不到成员表兜底放行防误伤；管理员/未指定项目放行） | ✅ 已修复并验证 |
| 4 | 中（P2） | 错误响应非 RESTful：401/403/404/413/500 全部返回 HTTP 200 + 业务 code。监控/网关无法按状态码告警，外部集成易误判成功 | 见 F5/F8 | fail 按业务码映射真实 HTTP 状态码（4xx/5xx 同值透传）；全局错误中间件透传 413 等；前端 error 拦截器提取响应体同构处理（401 自愈重试保留） | ✅ 已修复并验证 |
| 5 | 中（P2） | 工时越界未拒绝：overrides.hours=99 提交返回「记录成功」，未验证实际写入值（夹取 or 原样） | submit-work 带 overrides:{hours:99} → code=0 | 服务端硬校验 0-24，越界返回 400「工时需在 0-24 小时之间」 | ✅ 已修复并验证 |
| 6 | 低（P3） | /api/health 免鉴权暴露运行形态（mode=dingtalk、table:true、ai:true） | GET /api/health | 精简为 status+time（完整运行状态仅打印在服务端启动日志） | ✅ 已修复并验证 |
| 7 | 低（P3） | CORS 全开放（ACAO:*），与问题 1 叠加放大跨域攻击面 | OPTIONS 任意 Origin → 反射 * | CORS 收敛：仅放行自身来源与本地开发调试来源（支持 CORS_ORIGINS 环境变量扩展），其余跨域请求不返回 CORS 头 | ✅ 已修复并验证 |
| 8 | 低（P3） | jsapi-sign 免鉴权，暴露 agentId/corpId 并为任意来源签 JSAPI 签名 | GET /api/login/jsapi-sign?url=... | 移至鉴权后（/api/jsapi-sign，需 Bearer token）；前端调用同步携带登录态 | ✅ 已修复并验证 |
| 9 | 低（P3） | XSS 载荷原样入库（前端无 v-html 已安全，但钉钉表格侧与未来富文本渲染存在纵深隐患） | submit-work 带 `<img onerror>` → 原样存储 | 服务端入库前过滤 HTML 标签（submit-work 内容与 submit-plan 标题） | ✅ 已修复并验证 |
| 10 | 记录 | 会话为内存态，服务重启全员登出（本次重启已发生；钉钉内自动免登，用户基本无感） | 重启 pm2 | 维持现状（设计记录，不修）；前端已有 401 自动重登自愈机制 | 📌 记录在案 |

---

## 四、修复验证记录（2026-08-23，生产环境 101.37.210.127:3001）

| 验证项 | 方法 | 结果 |
|---|---|---|
| P0 访客登录拒绝 | `POST /api/login -d '{}'`（外网） | ✅ 返回「缺少免登授权码，请在钉钉内打开应用」，不再发访客会话 |
| 访客降权（代码面） | 本地正式模式登录 | ✅ 会话 isAdmin=false |
| P1 越权拦截 | 本地访客会话携带 `x-project-id: p_chengtou` 请求 /api/tasks | ✅ HTTP 403「你不在该项目成员表中」（真实调钉钉成员表判定） |
| P1 AI 兜底链 | 服务器直调 llm.chat | ✅ 主模型超时→自动切备用模型→19.6s 返回正常回答；失败路径返回明确错误而非空 answer |
| P2 状态码 | 外网/本地 curl 各错误端点 | ✅ 401/404/403/400 均为真实 HTTP 状态码 + 业务 code |
| P2 工时校验 | submit-work hours=99 | ✅ HTTP 400「工时需在 0-24 小时之间」 |
| P3 health | 外网 GET /api/health | ✅ 仅返回 status+time |
| P3 CORS | OPTIONS 三种 Origin | ✅ 外部来源无 ACAO；同源/开发来源回显 |
| P3 jsapi-sign | 无 token 请求 | ✅ HTTP 401；带 token 200；旧路径 /api/login/jsapi-sign 不再存在 |
| P3 XSS 过滤 | submit-work 带 `<img onerror>` | ✅ 入库内容标签已移除，正常文本保留 |
| 冒烟数据清理 | 钉钉删除 API + 本地落盘 | ✅ 表格 10→9 条，本地 10→9 条，临时脚本已删，无残留 |
| 服务健康 | pm2 重启后 | ✅ 正常启动，回载 9 条正常记录，前端新版本（index-kl-uAtjU.js）已上线 |

> 修复提交：`9cd35b7`（10 文件，+168/-51），已推送并部署至生产。

---

## 五、结论

- 功能面 44/45 通过，核心业务（日志提交/去重/解析、任务权限三道校验、计划成员校验、登录鉴权）全部符合设计
- 性能与兼容性全面达标，无优化项
- **问题 1-9 已全部修复并通过生产环境回归验证（见第四节），问题 10 为设计记录维持现状**；修复后遗留风险：智谱免费模型限流仍偶发，AI 查询依赖备用模型兜底，如需彻底稳定可考虑付费模型或通义千问双通道
